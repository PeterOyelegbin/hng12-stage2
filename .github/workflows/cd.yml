name: CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy API
        run: |
          ssh user@server "cd /path/to/api && git pull && docker-compose up -d --build"
